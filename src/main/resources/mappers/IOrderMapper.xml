<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.whjx.dao.IOrderMapper">

    <insert id="createOrder" parameterType="com.whjx.pojo.Order">
       insert into w_order values (#{orderId},#{payment},#{paymentType},#{postFee},#{status},#{createTime},#{updateTime},#{paymentTime},#{consignTime},#{endTime},#{closeTime},#{shippingName},#{shippingCode},#{openId},#{buyerMessage},#{buyerNick},#{buyerRate})

    </insert>



</mapper>